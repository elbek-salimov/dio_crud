import 'package:flutter/material.dart';

class DelScreen extends StatelessWidget {
  const DelScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
